# Virtual Environments
.venv/
venv/
env/
ENV/

# Python Cache & Compiled Files
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/

# Raw and Processed Datasets (Handled by DVC)
data/raw/*
!data/raw/*.dvc
!data/raw/.gitignore
data/processed/*
!data/processed/*.dvc

# Experiment Tracking (Week 2 - M3)
mlruns/
mlartifacts/
# Model artifacts are committed so downstream milestones (Week 3 serving)
# can load the packaged model directly after a git clone.
# Keep the generated comparison report (committed as a deliverable)
!reports/

# Credentials & Environment Variables
.env
kaggle.json
access_token

# IDE & System Files
.vscode/
.idea/
Thumbs.db
.DS_Store